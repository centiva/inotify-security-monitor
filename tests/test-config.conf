#!/usr/bin/env bash

WATCH_DIRS=(
    "/tmp"
)

WATCH_EXTENSIONS=(
    "php"
    "phtml"
    "sh"
    "js"
)

EXCLUDE_DIRS=()

EXCLUDE_FILES=()

EXCLUDE_EXTENSIONS=()

EXCLUDE_DIR_PATTERNS=(
    "*/cache/*"
    "*/vendor/*"
)

EXCLUDE_FILE_PATTERNS=()

EXCLUDE_PATH_PATTERNS=()

LOG_FILE="/tmp/test-inotify.log"
QUEUE_FILE="/tmp/test-inotify.queue"

EMAIL_ENABLED=false
